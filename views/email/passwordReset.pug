extends baseEmail

block content 
  p Hi #{firstName},
    p Forgot your password? Submit a PATCH request with your new password and passwordConfirm to: #{url.url+"?url="+ url.token}.
  
    //- p (website for this actoin not yet implemented)
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
      tbody
        tr
          td(align='left')
            table(role='presentation', border='0', cellpadding='0', cellspacing='0')
              tbody
                tr
                  td
                    //- a(href=`${url}`, target='_blank') Rest your password
                    a.btn.btn-primary(href=`${url.url}?url=${url.token}`, target='_blank') Reset your password
    p If you did't forget your password, please ignore this email!
    p - Amr Mustafa, CEO